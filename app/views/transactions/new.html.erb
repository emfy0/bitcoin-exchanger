<div class="container">
  <div class="row justify-content-center" data-market-fee="<%= @market_fee %>"
    data-miner-fee="<%= sprintf("%.8f", @miner_fee) %>" data-ust-to-btc="<%= sprintf("%.8f", @ust_exchange_rate) %>">
    <%= render 'form', transaction: @transaction %>
  </div>
</div>
